<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.matt.authinterceptor.sample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".withdagger.di.AuthApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="MissingApplicationIcon,UnusedAttribute,GoogleAppIndexingWarning">
        <activity android:name=".simple.SimpleActivity" />
        <activity android:name=".withdagger.di.ActivityWithDagger"></activity>
    </application>

</manifest>